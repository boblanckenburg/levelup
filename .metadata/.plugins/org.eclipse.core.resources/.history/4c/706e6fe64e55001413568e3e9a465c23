
var percentage = (int)(Math.random() * 100);

var progress_fill_element = document.getElementById("levelcounter-progressbar");
var percentage_element = document.getElementById("levelcounter-percentage");

percentage.innerHTML = percentage + "%";
progress_fill.style.width = percentage + "%";

alert( "percentage " + percentage );
